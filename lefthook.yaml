post-checkout:
  jobs:
    - name: update submodules
      run: git submodule update --init --recursive
pre-commit:
  jobs:
    - name: check staged files
      run: biome check --write {staged_files}
      glob: "*.{js,cjs,mjs,jsx,ts,cts,mts,tsx,json,jsonc}"
      stage_fixed: true
